---
import PaginationRenderer, {
  type PaginationRendererProps,
} from './PaginationRenderer';

type Props = PaginationRendererProps;

const { ...rest } = Astro.props;
---

<PaginationRenderer {...rest} className="tailwind-preflight" client:load />

<style is:global lang="scss">
  @use 'sass:meta';

  .tailwind-preflight {
    @include meta.load-css('@styles/tailwind-preflight');
  }
</style>
