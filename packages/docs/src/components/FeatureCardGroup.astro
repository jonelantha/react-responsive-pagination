---

---

<ul>
  <slot />
</ul>

<style lang="scss">
  @use '@styles/_vars.scss';

  ul {
    list-style: none;
    padding: var(--v-spacing) vars.$landing-padding-inline;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(13rem, 1fr) minmax(13rem, 1fr));
    gap: 16px;

    @media screen and (max-width: 35rem) {
      grid-template-columns: 1fr;
    }
  }
</style>
