---
import PaginationContainer from './PaginationContainer.astro';
import type { Props as PaginationContainerProps } from './PaginationContainer.astro';

type Props = PaginationContainerProps;

const props = Astro.props;
---

<PaginationContainer class="bootstrap-styles" {...props}>
  <slot />
</PaginationContainer>

<style is:global lang="scss">
  @use 'sass:meta';

  .bootstrap-styles {
    @include meta.load-css('react-responsive-pagination/themes/bootstrap');
  }
</style>
