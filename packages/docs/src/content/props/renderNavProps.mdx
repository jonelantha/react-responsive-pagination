import PropDef from '@components/PropDef.astro';

<Fragment>
  <PropDef name="renderNav" type="boolean" defaultValue="true">
    When set to `false` the nav buttons (**«**/**»**) will not be rendered. Defaults
    to `true`
  </PropDef>
</Fragment>
