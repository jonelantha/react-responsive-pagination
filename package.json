{
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "build": "cd packages/react-responsive-pagination && npm run build",
    "release": "cd packages/react-responsive-pagination && semantic-release"
  }
}
