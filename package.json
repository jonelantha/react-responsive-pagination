{
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "build": "yarn workspace react-responsive-pagination run build",
    "test-dev": "yarn workspace browser-test test-dev",
    "dev-test-harness": "yarn workspace browser-test start",
    "test": "yarn workspace browser-test run test"
  }
}
